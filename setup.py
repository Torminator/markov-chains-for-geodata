from setuptools import setup

setup(
   name='markovchains',
   version='0.1',
   description='This is a general purpose simple markov chain implementation.',
   author='Schick, Torben',
   packages=['markovchains'],
   install_requires=['numpy'], 
)
